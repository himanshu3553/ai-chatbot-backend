fastapi==0.103.2
uvicorn[standard]==0.23.2
pydantic==2.3.0
python-multipart==0.0.6
# Alternative: Use pre-compiled wheels to avoid build issues
# Uncomment the line below if you still encounter build errors
# --only-binary=all
